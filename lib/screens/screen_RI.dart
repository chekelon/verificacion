import 'package:flutter/material.dart';

class ScreenRI extends StatelessWidget {
  const ScreenRI({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Container(
          child: Text("Screen RI.."),
        ),
      ),
    );
  }
}
