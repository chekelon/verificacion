import 'package:flutter/material.dart';

class ScreenBE extends StatelessWidget {
  const ScreenBE({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Container(
          child: Text("Screen BE.."),
        ),
      ),
    );
  }
}
